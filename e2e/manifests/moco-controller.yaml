apiVersion: apps/v1
kind: Deployment
metadata:
  name: myso-controller-manager
  namespace: myso-system
spec:
  template:
    spec:
      containers:
      - name: manager
        command:
        - /moco-controller
        image: controller:dev
        imagePullPolicy: Never
