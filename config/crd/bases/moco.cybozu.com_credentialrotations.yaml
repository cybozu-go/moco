---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: credentialrotations.moco.cybozu.com
spec:
  group: moco.cybozu.com
  names:
    kind: CredentialRotation
    listKind: CredentialRotationList
    plural: credentialrotations
    singular: credentialrotation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.rotationGeneration
      name: Generation
      type: integer
    - jsonPath: .status.observedRotationGeneration
      name: Observed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: CredentialRotation is the Schema for the...
        properties:
          apiVersion:
            description: APIVersion defines the versioned schema of this...
            type: string
          kind:
            description: Kind is a string value representing the REST...
            type: string
          metadata:
            type: object
          spec:
            description: CredentialRotationSpec defines the desired state...
            properties:
              discardOldPassword:
                description: DiscardOldPassword triggers the discard phase.
                type: boolean
              rotationGeneration:
                description: RotationGeneration is a monotonically increasing...
                format: int64
                type: integer
            type: object
          status:
            description: CredentialRotationStatus defines the observed...
            properties:
              observedRotationGeneration:
                description: ObservedRotationGeneration is the last...
                format: int64
                type: integer
              phase:
                description: Phase is the current rotation phase.
                type: string
              rotationID:
                description: RotationID is the UUID for this rotation cycle.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
